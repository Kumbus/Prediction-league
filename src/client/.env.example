# Copy to .env and fill in values for local development.
# VITE_ prefix required — only these vars are exposed to the browser bundle.

# API base URL (no trailing slash)
# Local dev: http://localhost:5185
# Production: https://prediction-league-api-0523444a.azurewebsites.net
VITE_API_BASE_URL=
