export function AppShell() {
  return (
    <main className="flex min-h-svh items-center justify-center p-6">
      <p className="text-muted-foreground">App shell — Phase 2 fills this in.</p>
    </main>
  )
}
